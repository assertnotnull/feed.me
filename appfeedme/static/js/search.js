var map;var latitude = 45.523668;var longitude = -73.655777;$(document).ready(function(){    $('#map').gmap3(        {action: 'init',            options: {                center: [latitude, longitude],                zoom: 10,                mapTypeControl: true,                navigationControl: true,                scrollwheel: true,                streetViewControl: true            }        }    )    $('#submit').click(function() {search()});    $('#boroughs').change(function() {search()});});function search() {	var cat = $('#search').val();    var loc = $('#boroughs option:selected').val().split(',');    latitude = loc[0];    longitude = loc[1];    if (cat.length != 0) {	    $.get('/search/' + cat + "/" + longitude + "," + latitude, showResults);    }}function showResults(data) {	$('#results').html();	$('#map').gmap3(        {action: 'clear'}    )	$('#results').html(data);    $('#carousel').carousel({        interval: 0    });}